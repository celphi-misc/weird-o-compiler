var __var = 3;

var val = __var;

function foo() {
    var bar = val << 2;
    __var += val * bar > 0 ? __var : val >> __var;
    return bar + __var;
}

function main(argc, argv)
{
    var a = "hello world";
    function hello(para1, para2) { return null; }
    if(a == "hello") {
        for(var i = 0; i < 10; i++)
        {
            print(a);
        }
    } else if(a[0] == 'h')
    {
        print(a);
    } else {
        print("foo");
    }
    return 0;
}